# CLAUDE.md

Guidance for Claude Code when working in this repository.

## Project

lasrs-cpp is a C++ wrapper (not a port) around the Rust crates las-rs,
laz-rs (parallel LAZ) and copc-rs. See docs/ARCHITECTURE.md for the design
and docs/ROADMAP.md for milestones.

## Layout

- `rust/` - shim crate `lasrs_ffi` (staticlib, extern "C")
- `include/lasrs/lasrs.h` - C API, generated by cbindgen, committed
- `include/lasrs/lasrs.hpp` - header-only C++17 wrapper
- `tests/`, `examples/` - C++ tests and examples

## Commands

```
cmake --preset release
cmake --build --preset release
ctest --preset release
cargo fmt --manifest-path rust/Cargo.toml
cbindgen --config rust/cbindgen.toml --crate lasrs_ffi --output include/lasrs/lasrs.h rust
```

## Rules

- 7-bit ASCII only in all code and docs.
- SPDX header `MIT OR Apache-2.0` on new source files.
- FFI: batch-only API, no per-point calls; caller-owned buffers; opaque
  handles freed by Rust; every export wrapped in catch_unwind and returns
  a status code.
- Regenerate and commit `lasrs.h` after changing exports.
- New Rust dependencies must have permissive licenses (cargo deny).
